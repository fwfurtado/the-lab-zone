apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: proxmox-mcp-token-id
  namespace: ai
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: onepassword
    kind: ClusterSecretStore
  target:
    name: proxmox-mcp-token-id
    creationPolicy: Owner
  data:
    - secretKey: value
      remoteRef:
        key: proxmox-mcp
        property: readonly-token-id
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
        nullBytePolicy: Ignore
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: proxmox-mcp-token-secret
  namespace: ai
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: onepassword
    kind: ClusterSecretStore
  target:
    name: proxmox-mcp-token-secret
    creationPolicy: Owner
  data:
    - secretKey: value
      remoteRef:
        key: proxmox-mcp
        property: readonly-secret
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
        nullBytePolicy: Ignore
