# apps/ai/triage-webhook/manifests/external-secret.yaml
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: triage-webhook
  namespace: ai
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: onepassword
    kind: ClusterSecretStore
  target:
    name: triage-webhook
    creationPolicy: Owner
  data:
    - secretKey: LITELLM_KEY
      remoteRef:
        key: Labot
        property: litellm-token
    - secretKey: SLACK_BOT_TOKEN
      remoteRef:
        key: Labot
        property: bot-token
    - secretKey: GARAGE_ACCESS_KEY
      remoteRef:
        key: garage-triage
        property: accessKeyId
    - secretKey: GARAGE_SECRET_KEY
      remoteRef:
        key: garage-triage
        property: secretKey
