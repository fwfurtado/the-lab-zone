apiVersion: v1
kind: Service
metadata:
  name: headroom
  namespace: ai
  labels:
    app.kubernetes.io/name: headroom
spec:
  selector:
    app.kubernetes.io/name: headroom
  ports:
    - name: http
      port: 8787
      targetPort: http
