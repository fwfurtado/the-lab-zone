# OTel Collector — DaemonSet no cluster (chart 0.158.1)
# Fase 5: recebe métricas/logs via OTLP e encaminha para VictoriaMetrics/VictoriaLogs
# Traces de IA: exportados para o Langfuse (projeto de teste zone-test) via otlphttp.
# (VictoriaTraces full-stack fica para a Fase 7)

mode: daemonset

# Obrigatório desde chart ~0.127.x — sem isso o helm template falha
# otelcol-k8s é a distribuição recomendada para Kubernetes (inclui k8s receivers)
image:
  repository: ghcr.io/open-telemetry/opentelemetry-collector-releases/opentelemetry-collector-k8s

command:
  name: otelcol-k8s

presets:
  # Adiciona k8s metadata em todos os spans/métricas/logs automaticamente
  # (k8s.pod.name, k8s.namespace.name, k8s.node.name, etc.)
  kubernetesAttributes:
    enabled: true
    extractAllPodLabels: false
    extractAllPodAnnotations: false
  # Coleta logs de todos os containers via filelog receiver
  logsCollection:
    enabled: true
    includeCollectorLogs: false
    storeCheckpoints: false

# Credencial OTLP do Langfuse (ESO/Infisical). Deve conter a chave
# LANGFUSE_ZONE_TEST_OTEL_AUTH = "Basic <base64(pk:sk)>" do projeto zone-test.
# Injetada como env no pod para o ${env:...} do exporter resolver.
extraEnvsFrom:
  - secretRef:
      name: langfuse-zone-test

config:
  receivers:
    otlp:
      protocols:
        grpc:
          endpoint: ${env:MY_POD_IP}:4317
        http:
          endpoint: 0.0.0.0:4318
    # Removendo jaeger/zipkin/prometheus — não usamos
    jaeger: null
    zipkin: null
    prometheus: null

  processors:
    batch:
      timeout: 10s
      send_batch_size: 1024
    memory_limiter:
      check_interval: 5s        # era 1s no values antigo; default do chart é 5s (mais correto)
      limit_percentage: 75
      spike_limit_percentage: 15
    # Descarta spans de healthcheck (GET .../healthz): a borda pole o núcleo a
    # cada ~15s e, sem trace pai, cada poll vira um trace ÓRFÃO no Langfuse.
    # Stopgap enquanto a imagem 0.7.x da borda (com WithFilter no source) não
    # sobe — depois pode sair. error_mode: ignore => span sem url.full é mantido.
    filter/drop_health:
      error_mode: ignore
      traces:
        span:
          - 'IsMatch(attributes["url.full"], ".*/healthz$")'
    # k8sattributes é injetado automaticamente pelo preset kubernetesAttributes

  exporters:
    otlphttp/metrics:
      endpoint: http://vmsingle-victoria-metrics-vmks.observability.svc.cluster.local:8428/opentelemetry
      tls:
        insecure: true

    otlphttp/logs:
      endpoint: http://victoria-logs-vls-server.observability.svc.cluster.local:9428/insert/opentelemetry
      tls:
        insecure: true

    # Traces de IA -> Langfuse (projeto de teste zone-test).
    # HTTP-only: o Langfuse não aceita gRPC; o exporter anexa /v1/traces ao endpoint base.
    otlphttp/langfuse-zone-test:
      endpoint: http://langfuse-web.ai.svc.cluster.local:3000/api/public/otel
      headers:
        Authorization: ${env:LANGFUSE_ZONE_TEST_OTEL_AUTH}   # "Basic <base64(pk:sk)>"
        x-langfuse-ingestion-version: "4"

    # debug mantido durante a validação: `kubectl logs` mostra os spans chegando.
    # Pode sair do pipeline de traces depois que o zone-test estiver confirmado.
    debug:
      verbosity: basic

  service:
    pipelines:
      metrics:
        receivers: [otlp]
        processors: [memory_limiter, batch]
        exporters: [otlphttp/metrics]
      logs:
        receivers: [otlp, filelog]  # filelog injetado pelo preset logsCollection
        processors: [memory_limiter, batch]
        exporters: [otlphttp/logs]
      traces:
        receivers: [otlp]
        processors: [memory_limiter, filter/drop_health, batch]
        exporters: [otlphttp/langfuse-zone-test, debug]

# Porta metrics habilitada para o VMAgent scrape o próprio collector
ports:
  metrics:
    enabled: true
    containerPort: 8888
    servicePort: 8888
    protocol: TCP
  # Desabilita portas que não usamos
  jaeger-compact:
    enabled: false
  jaeger-thrift:
    enabled: false
  jaeger-grpc:
    enabled: false
  zipkin:
    enabled: false

# GOMEMLIMIT = 80% de limits.memory (feature do chart, altamente recomendado)
useGOMEMLIMIT: true

resources:
  requests:
    cpu: 50m
    memory: 128Mi
  limits:
    memory: 256Mi

# Tolera o taint do nó GPU para coletar telemetria do worker-3-gpu também.
# O taint nvidia.com/gpu=present:NoSchedule existe para barrar workload genérico —
# DaemonSet de telemetria é exceção: queremos observabilidade JUSTO no nó mais quente
# (Ollama, DCGM, inferência). Sem isso o controller não agenda o agent lá e o
# pod pré-taint vira misscheduled (KubeDaemonSetMisScheduled + RolloutStuck).
tolerations:
  - key: nvidia.com/gpu
    operator: Equal
    value: present
    effect: NoSchedule
