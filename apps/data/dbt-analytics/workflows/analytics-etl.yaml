# Run-once do ETL (migrate -> dbt build). Substitui a execução manual da Etapa 3.
#   kubectl create -f test/wf-analytics-etl.yaml
#   kubectl -n data get wf -w
# ou, sobrescrevendo a revision (ex. testar um branch/SHA):
#   argo submit -n data --from workflowtemplate/analytics-etl -p revision=<branch|sha> --watch
apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: analytics-etl-
  namespace: data
spec:
  workflowTemplateRef:
    name: analytics-etl
  # arguments:           # opcional: sobrescreve a revision default do template
  #   parameters:
  #     - name: revision
  #       value: main
